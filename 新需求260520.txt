1. 强制休息完毕解锁屏幕时，如果有打开的用户未关闭的3分钟后休息的提示窗口，那么关闭它。调整设置窗口高度，以显示新加入到配置项。
2. 设置属性窗口中在“允许推迟次数”之后增加配置项--“键盘鼠标静止X分钟后暂停计时”的选项，默认勾选，X为输入框，默认为5，可修改，数值范围3-30。同步修改工作时倒计时窗口逻辑，勾选后，键盘鼠标静止X分钟后暂停计时，直到用户重新动键盘或鼠标后再开始一个新周期计时，是重新计时不是继续原有的倒计时。
3. 内存占用优化，刚开始运行时占用内存800-100M，后续运行时间长后，内存占用180M，尝试优化减少内存占用。