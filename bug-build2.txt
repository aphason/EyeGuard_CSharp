1. 设置属性窗口中， 允许推迟次数 下方增加选项 软件全屏运行时暂停计时，只有勾选了才启用，否则不启用，修改相应逻辑。强制休息模式的选项文字改为：非强制、一般强制（休1分钟可解锁）、完全强制（休息过程中不可解锁）。下方提示文字"开启后休息过程中无法跳过"去掉。
2. 全屏运行判断逻辑是否有问题？命令行应用全屏（FullScreen）运行模式的特征：应用独占了一个屏幕大小，窗口没有右上角的最大化、最小化、关闭窗口的按钮。如果软件全屏运行时暂停计时选项启用，即使全屏运行的窗口不是激活状态或鼠标点击了其他窗口，也暂停计时。最大化运行的窗口不能暂停计时。
3. 工作中倒计时窗口调整，去掉图标，文字居中显示，窗口大小调小，可用去掉“下次休息”的文字；
4. 工作中 点击了右键菜单总在最前显示，菜单前方图标显示✓，取消最前显示的菜单前方取消✓显示；点击了右键菜单取消最前显示，菜单前方图标显示✓，对应的总在最前显示菜单取消显示✓。
