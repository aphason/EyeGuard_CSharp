<Window x:Class="EyeGuard.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="爱眼卫士 设置" Width="550" Height="540"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="ToolWindow" Background="#161b22"
        Foreground="#c9d1d9" FontSize="13">
    <Window.Resources>
        <!-- ComboBoxItem 样式 -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#0d1117"/>
            <Setter Property="Foreground" Value="#c9d1d9"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>

        <!-- ComboBox 样式（重写模板） -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0d1117"/>
            <Setter Property="Foreground" Value="#c9d1d9"/>
            <Setter Property="BorderBrush" Value="#30363d"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- 触发器定义 -->
                            <ToggleButton x:Name="ToggleButton"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      Foreground="{TemplateBinding Foreground}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"/>

                            <!-- 内容显示区 -->
                            <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="3,3,23,3"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>

                            <!-- 下拉列表 Popup -->
                            <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                                <!-- 关键：这里设置下拉列表的背景色 -->
                                <Border x:Name="DropDownBorder"
                                    Background="#0d1117"
                                    BorderBrush="#30363d"
                                    BorderThickness="1"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer x:Name="DropDownScrollViewer"
                                              Margin="4,6,4,6"
                                              SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="爱眼卫士 设置" FontSize="16" FontWeight="600"
                   Foreground="#e6edf3" Grid.Row="0" Margin="0,0,0,16"/>

        <!-- Settings body -->
        <StackPanel Grid.Row="1" Margin="0,0,16,0">
            <!-- Auto start -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="开机自启动" Foreground="#e6edf3"/>
                    <TextBlock Text="系统启动时自动运行" Foreground="#8b949e" FontSize="11"/>
                </StackPanel>
                <CheckBox x:Name="AutoStartCheck" Grid.Column="1" VerticalAlignment="Center"
                          Foreground="#e6edf3"/>
            </Grid>

            <Separator Background="#30363d" Margin="0,0,0,12"/>

            <!-- Work duration -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="工作时间" Foreground="#e6edf3"/>
                    <TextBlock Text="两次休息之间的工作时长" Foreground="#8b949e" FontSize="11"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="WorkDurationBox" Text="50" Width="50" TextAlignment="Center"
                             Background="#0d1117" Foreground="#c9d1d9" BorderBrush="#30363d"
                             Padding="4,3"/>
                    <TextBlock Text="分钟" Margin="6,0,0,0" Foreground="#8b949e" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Rest duration -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="休息时间" Foreground="#e6edf3"/>
                    <TextBlock Text="每次休息的持续时间" Foreground="#8b949e" FontSize="11"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="RestDurationBox" Text="5" Width="50" TextAlignment="Center"
                             Background="#0d1117" Foreground="#c9d1d9" BorderBrush="#30363d"
                             Padding="4,3"/>
                    <TextBlock Text="分钟" Margin="6,0,0,0" Foreground="#8b949e" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Postpone count -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="允许推迟次数" Foreground="#e6edf3"/>
                    <TextBlock Text="每次休息周期内可推迟的次数" Foreground="#8b949e" FontSize="11"/>
                </StackPanel>
                <ComboBox x:Name="PostponeCombo" Grid.Column="1" Width="60" VerticalAlignment="Center"
                          SelectedIndex="2" >
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="4"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="6"/>
                </ComboBox>
            </Grid>

            <!-- Idle pause (新增) -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="键盘鼠标静止后暂停计时" Foreground="#e6edf3"/>
                    <TextBlock Text="勾选后键盘鼠标静止时暂停工作倒计时" Foreground="#8b949e" FontSize="11"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox x:Name="PauseOnIdleCheck" VerticalAlignment="Center"
                              Foreground="#e6edf3" Margin="0,0,8,0"/>
                    <TextBlock Text="静止" Margin="0,0,4,0" Foreground="#8b949e" VerticalAlignment="Center"/>
                    <TextBox x:Name="PauseOnIdleMinutesBox" Text="5" Width="50" TextAlignment="Center"
                             Background="#0d1117" Foreground="#c9d1d9" BorderBrush="#30363d"
                             Padding="4,3"/>
                    <TextBlock Text="分钟后暂停" Margin="6,0,0,0" Foreground="#8b949e" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Pause on fullscreen -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="软件全屏运行时暂停计时" Foreground="#e6edf3"/>
                    <TextBlock Text="勾选后全屏应用运行时暂停工作倒计时" Foreground="#8b949e" FontSize="11"/>
                </StackPanel>
                <CheckBox x:Name="PauseOnFullscreenCheck" Grid.Column="1" VerticalAlignment="Center"
                          Foreground="#e6edf3"/>
            </Grid>

            <!-- Enable logging -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="记录日志" Foreground="#e6edf3"/>
                    <TextBlock Text="记录应用全屏监测情况到日志文件" Foreground="#8b949e" FontSize="11"/>
                </StackPanel>
                <CheckBox x:Name="EnableLoggingCheck" Grid.Column="1" VerticalAlignment="Center"
                          Foreground="#e6edf3"/>
            </Grid>

            <Separator Background="#30363d" Margin="0,0,0,12"/>

            <!-- Force mode -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="强制休息模式" Foreground="#e6edf3"/>
                </StackPanel>
                <ComboBox x:Name="ForceModeCombo" Grid.Column="1" Width="100" VerticalAlignment="Center">
                    <ComboBoxItem Content="非强制"/>
                    <ComboBoxItem Content="一般强制（休1分钟可解锁）"/>
                    <ComboBoxItem Content="完全强制（休息过程中不可解锁）"/>
                </ComboBox>
            </Grid>

            <Border Background="#0d1117" BorderBrush="#30363d" BorderThickness="1"
                    Padding="10,8" Margin="0,4,0,0">
                <TextBlock x:Name="ForceModeHint" Text="非强制：休息过程中可随时解锁"
                           Foreground="#8b949e" FontSize="11"/>
            </Border>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="取消" Width="70" Padding="6,4" Margin="0,0,8,0"
                    Background="#21262d" Foreground="#c9d1d9" BorderBrush="#30363d"
                    Click="Cancel_Click"/>
            <Button Content="保存" Width="70" Padding="6,4"
                    Background="#238636" Foreground="#ffffff" BorderBrush="#238636"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
